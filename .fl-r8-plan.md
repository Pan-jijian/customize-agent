# 丰乐镇第八轮修复预案（基于第六轮 35 条 error 只读分析）

## 已确认第六轮 35 error 分类与修复映射

### 第七轮已修复（10 条，预计消除）
- #0/#8 灭火器 4/20 部位误判 → qualifyLocationHit（documentIntegrityChecks.ts）
- #6 工期总日历天数零命中 → scoringResponseTail 新分支（tenderRequirements.ts）
- #7 预付款部分响应 → SCORING_FIX_PAYMENT_TERM_RE（tenderRequirements.ts）
- #10/11/12 自伤三形态 → fixSelfUnderminingCandidates A20（documentIntegrityChecks.ts）
- #14 断行残片 → surfaceFixRound2（documentPipeline.ts）
- #33 劳动力 86/32/20 → fullDimensionReviewSpec.txt 豁免
- #34 空响应句 → scoringResponseTail 9 分支 + fallback 主题词（tenderRequirements.ts）

### 第八轮 A 类：确定性修复（提取/检测器侧）
1. **事实提取污染 6 条（#1-#5、#13）** — factsModel.ts
   - 根因：XLS 单元格坐标（R6C3COL3/R3C1/R6C5 等）、PDF 串行噪声（"90日历天2.9"、
     "…2.6建设规模：…"）、表头串（"第页共页…单项工程名称金额(元)…"）混入事实值
   - 修复：isValidProjectBasicFactValue 通用过滤 `/R\d+C\d+|第\s*\d+\s*页.*共|金额\s*[（(]\s*元|暂估价|不可竞争费/u`；
     schedule_requirement 在 "日历天|天|个月|月|年" 后截断并拒绝含"招标范围|建设规模"；
     project_location 拒绝含后续字段名（"建设规模|招标范围|计划工期"）并在字段名处截断；
     owner 拒绝 R\d+C\d+ 开头；project_name 拒绝含金额/坐标/页签词
   - 防御：factConsistencyService.ts comparableValue 同口径过滤（值含坐标/表头串 → ''）
2. **合同条款数字反查误判 1 条（#26：28/29/56天）** — qualityValidation.ts
   - 根因：违约金条款"按合同价款万分之五计付"、结算条款"28天内提交竣工结算申请单"
     被 scope 分类为编造总量口径（异步语义分类器升级或上下文误判）
   - 修复：generatedFactTokenClass 加合同条款语境降级
     `if (/(?:违约金|合同价款|结算申请|解除合同|发包人|承包人|万分之|按合同)/u.test(normalized)) return 'soft';`
3. **汽车吊 1vs2 台（#9）** — 表格行修复
   - 根因：fl6-md @37835/@39885 两处表格写 1台，全文主导口径 2台；跨章数值修复漏修表格行
   - 待查：跨章一致性定点修复器是否覆盖表格行；若未覆盖则扩展

### 第八轮 B 类：评审轮吞吐（第六轮检出 72 处只修 7 处）
- fullDimensionReview.ts：REVIEW_REPAIR_CHAPTER_MAX=3、REVIEW_ISSUES_PER_REPAIR=3
  → 提升至 6/5；修复后仍有否决级/高风险时追加第二轮修复循环

### 第八轮 C 类：写作侧质量
- 模板化重度（套话句占比 27.4% > 10%、重难点归因+量化双达标 0% < 50%）
  → 写作任务书/章节生成 prompt 强化套话禁写与重难点归因量化要求

### 第八轮 D 类：内容补写兜底（确定性）
- 工伤保险句（documentIntegrityChecks.ts 检测但修复轮漏修）
- 应急预案四要素（组织体系/处置程序/演练/物资保障）
- 主要施工内容 ≥5 专业工程块 + 三要素 + 事实细度（constructionOrgQualityRules）
- 精确参数落位（抽查池 evidence token 使用率 ≥50%）

### 第八轮 E 类：规划一致性
- 缺少规划小节 ×3（工程重难点识别与应对/主要分部分项工程施工方案/资源配置计划）
  → 查 chapterPlanner.sections 与 Writer 实际输出的标题一致性
- 主要施工方法 分部分项工程施工方案小节缺失或标题结构异常

## 评分路径推算（目标 90+）
- weighted = completeness*0.30 + specificity*0.25 + compliance*0.20 + executability*0.15 + normalization*0.10
- overall = weighted * min(1, uniqueness/90)
- deliveryProbability = overall - blockingIssues*8，passed 需 ≥95 且 blocking=0
- 结论：blocking 必须 0；各维度需 ≥90 且 uniqueness ≥90
- 第六轮：weighted 45.8、uniqueness 81 → overall 41
- normalization 0→90 贡献 +9；blocking 25→0 贡献 +200（dp 口径）
- 核心杠杆：normalization error 数（15 个 norm-matching error，每 error 扣 8 分）
