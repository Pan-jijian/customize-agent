/**
 * 解析器版本戳（G 线 P2-6）。
 *
 * **为什么必须有**：`change-tracker` 的重解析判据只覆盖「文件本身变了」（mtime / 大小 /
 * 内容哈希）与若干「明显坏掉」的形态（`chunkCount === 0`、`status === 'error'`、
 * pdf 仅 1 片、metadata-only）。于是**解析器的改进对存量库完全不可见**——
 * 修好了图纸 MTEXT 格式码剥离（P2-7）、PDF 表格智能表头（P2-9），已入库的旧切片
 * 仍带着旧的错误内容，直到该文件本身被改动才重解析。唯一补救是人工触发全量重建，
 * 而这不可持续（本仓已经这样做过一次）。
 *
 * **用法**：任何影响「解析/清洗产出内容」的改动都必须递增本版本号。存量记录在下一次
 * 变更扫描时即被判为待重解析（`packages/knowledge/src/core/change-tracker.ts`）。
 * 据此，「解析器修好了」第一次变得可自动传播。
 *
 * **注意副作用**：首次引入本戳时，所有历史记录都没有该字段 ⇒ 会触发**一次性全量重解析**。
 * 这是有意为之（否则本仓前几轮解析修复永远无法自动生效），但要有心理预期：
 * 升级后第一次索引扫描的耗时与一次全量重建相当。
 */
/**
 * 4.61 递增：本版含三项**影响解析/清洗产出内容**的改动，必须靠版本戳传播到存量库——
 * 1. 切分器末级分隔符 `/\s+/` → `/\n/`（`text-chunker.ts`）：修复"切开再拼回"不保真导致的
 *    **CAD 键值行空格变换行**（实测落库 chunk 690 字符 / 79 换行 / 0 空格，图层/块/实体类型
 *    三组结构在存储层被拆散）；
 * 2. 删除不可达的第二份 DXF 实现（`content-extractor.ts`，口径与主力路径不一致）；
 * 3. 要求提取的载体闸（`stageUnderstanding`）——该项影响的是**下游提取**而非入库文本，
 *    由 `tenderRequirementsCacheKey` 的证据指纹覆盖，不依赖本戳，但一并记在此处备查。
 */
export const PARSER_VERSION = 'kb-parser-2026.09.24-structure';
