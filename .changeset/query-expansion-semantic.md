---
"@customize-agent/server": patch
---

查询扩展语义化：标题→检索查询类别的判别从正则枚举迁移为 bge 语义分类（正则仅作降级兜底）

- `planDocument` 章节/小节标题按 6 类查询类别原型（安全/工期/质量/资源/概况/部署）做批量嵌入余弦判别，语义命中即附加对应查询词——新形态标题（如「确保人、材、机的保障体系与措施」「劳动力配置保障体系」）不再依赖正则枚举覆盖
- 确定性正则保留为降级兜底（语义模型不可用/`DOCUMENT_QUERY_EXPANSION_SEMANTIC=0` 时回退），语义与正则结果取并集；顿号形态「人、材、机」正则兼容保留
- 标题嵌入复用全局 LRU 缓存；`planDocument` 变更为异步（调用点已同步更新）
- 更新 agentPlanner.test.ts：9 个单测覆盖语义分类贡献、正则降级回退、env 开关、退化小节兜底
