---
'@customize-agent/server': patch
---

4.56.6：多值冲突残留三条（标签当值 / 项目粗名 / 区划简称）

`doc-1790166151755-4456e86c` 综合 88，事实维度由 40 升到 52（8 条「答疑修正未落实」已消除），
残留 3 条「存在多个值」逐条根治——都是「同一事实的不同粒度/形态被当成两个值」，且都含一个伪值：

- **标签当值**：`计划工期` 的第二个"值"是 `标段工程工期`——招标文件用表格呈现工期，那是**行标题**。
  它没有信息量，却因形态为 plain 而**打掉时长/日期的槽位分桶**（分桶要求组内值全部可判形态），
  把「330日历天」与「开工日期」重新拉回全量互比。判据（形态不查名单）：值不含数字、
  长度 ≤ 标签+6 字、以标签尾部 2 字收尾 ⇒ 是标签而非取值。
- **项目粗名吸收**：`巢湖市光电新能源产业园项目`（图纸里的粗名）是完整名的前缀，以 `项目/工程/标段`
  收尾即判粗名形态，并入长值。
- **区划简称折叠 + 前缀吸收扩展**：`经济开发区` ≡ `经开区` 归一后，`巢湖市居巢经开区` 恢复为
  `巢湖市居巢经开区义成路与南外环路交口北侧` 的前缀；行政区划后缀前缀吸收的长度上界 6 → 12 字。

三条防过度断言同步锁定：两个不同项目名 / 两个不同区划 / 两个不同时长仍必须报。

测试：新增 5 项；typecheck/lint 干净；全量 14,993 passed | 5 skipped。
