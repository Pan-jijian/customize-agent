---
"@customize-agent/server": patch
---

修复文档"已生成正文却被导出门禁阻断"的系统性缺陷：①Final Gate 深度阻断线不得超过修复验收线（历史缺陷：主要施工方法阻断线 1760 > 修复验收线 1200，补写 1715 字达标替换后重算仍被阻断，整篇生成失败）；②Final Gate 深度检查改为 fuzzy 标题提取，与 Reviewer 同口径，避免小节标题语义重写被误报为"正文不足 0 字"；③Final Gate 修复循环化（最多 3 轮、每轮最多 4 个候选），避免候选超限残留 error 直接阻断；④Repairer 修复循环不收敛修复：warning 级"正文不足"纳入修复范围、补写目标对齐 Reviewer 深度通过线、批量并发补写串行落位；⑤系统审查修复：补写深度查表按承接锚点标题解析（标题重写小节不再查表得 0）、Final Gate 提取 exact 优先 fuzzy 兜底（相似标题不漏判不误报）、批量补写改 allSettled（单小节异常不丢整批）、Reviewer 达标小节静默不再报"正文不足"（否则 Repairer 每轮全量补写永不收敛）、补写落位同时尝试承接标题（标题重写场景不再插入重复小节）
