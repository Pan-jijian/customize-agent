---
'@customize-agent/server': patch
---

修复检索段语义嵌入 CPU 瓶颈：章节证据语义排序全量本地嵌入（6 章 × ~1.5 万条 ≈ 9 万条）导致生成卡死（实测 61 分钟仅 3/6 章成稿）。证据切片级文本纳入全局嵌入缓存（阈值 64→2000 字、容量 2000→40000，跨章共享命中），语义排序前置词面粗筛（默认 top 3000 候选，env DOCUMENT_SEMANTIC_TOP_CANDIDATES 可调），嵌入量降低约 89%；修正主题块规划「LLM 调用异常」误导文案（细目 ≤8 条跳过 LLM 规划为设计行为）。
