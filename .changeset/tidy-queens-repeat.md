---
'@customize-agent/server': patch
---

修复轮口径对齐与成稿质量收敛（4.12.12）：

- 判别器补强：括号悬置/「以下简」截断/数字单位参数/评标程序动作 4 规则拦截碎片标题
- 大纲出口最终清洗升级为指令式判别超集，核心词提取剥离残余条款编号
- 成稿去重：跨层级同名整块去重（修复首个 H2 块漏扫）+ 同小节相邻重复段落去重
- telemetry 口径对齐：质量汇总先累加再构建报告，阻断数以导出门禁实际残留为准
- 两可表述阻断（斜杠并列/括号悬置）+ 基坑深度数值锁定 + 劳动力反向口径并入数值冲突检测
- 表格占位符确定性删行与「约N」归一 + 正文奖项白名单（杜撰奖项即阻断）
- 嵌入缓存分层：短文本缓存、长文本不查不写不计数，命中率口径净化
- LLM JSON 重试收敛：重试次数 1→2，截断类失败放大 maxTokens 1.5 倍
