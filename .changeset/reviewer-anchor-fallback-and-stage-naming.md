---
"@customize-agent/server": patch
---

修复徽光阁二轮实测暴露的 Review 修复循环缺陷与前端节点展示问题：

- **Reviewer 锚点回退**：plannedCoverage 锚点（规划重写 H4 标题）与块成稿实际标题不一致时（块级降级/成稿标题偏差），Reviewer 提取为空回退按细目原始标题提取，与 Repairer 落位口径对齐，消除"补写已落位但仍报正文不足"的 3 轮修复空转（章 1 实测 2 个阻断问题不收敛）
- **Repairer 工作包 H4 吞并**：小节标题后紧跟同级 H4 工作包的结构特征也触发吞并边界扩展，避免每轮补写追加一组新工作包形成 H4 成对重复（安全管理目标等标题劣化）
- **Repairer 阶段消息修正**：修复范围为 warning 级深度缺口（0 个阻断问题）时，消息改为"正在定向补写 N 个深度不足小节"，不再出现"发现 0 个阻断问题，正在定向修复"的矛盾展示
- **前端节点命名**：Agent Reviewer 节点展示为"LLM 审查"、Agent Repairer 展示为"LLM 优化"，不再统一叫"LLM 审查优化"；执行阶段列表同步优先使用 stage.title 与 subtitle
