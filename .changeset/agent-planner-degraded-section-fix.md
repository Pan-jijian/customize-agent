---
"@customize-agent/server": patch
---

修复真实生成回归：无预设小节章节（如「确保人、材、机的保障体系与措施」）章节任务未就绪导致 6 章只生成 5 章、导出门禁不通过的问题

- `semanticQueryExpansions` 资源类兜底正则兼容「人、材、机」顿号形态（原 `/人材机/` 仅匹配连续字符，顿号标题失去兜底查询，小节证据匹配全空）
- `planChapterTask` 退化小节兜底：模板无预设小节时小节=章节标题，章节级证据/图谱上下文天然属于该小节，词面未命中不再误判「缺少事实、图谱或证据支撑」
- 新增 agentPlanner.test.ts 7 个单测覆盖退化小节与顿号形态回归保护
