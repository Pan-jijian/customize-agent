---
'@customize-agent/server': minor
---

评分项要求提取治理（round-26）：字段级缺口检测与定向补提闭环覆盖全部评分项要求字段（必提 6 字段 + 特殊质量标准/前附表响应条款/禁编日期/禁止性要求），主提取+窄通道后任一字段缺失即触发句级窗口聚焦补提（最多 2 轮），stillGaps（真漏提）与 noEvidenceGaps（资料无此要求）语义互斥；按项目需求移除评标办法（evaluationScheme）与篇幅要求（pageLimit）字段——提取链路（主提取 prompt/schema、补提闭环、合并函数、写作规则、进度摘要）零残留。
