---
'@customize-agent/server': patch
---

4.56.4：事实维度归零悬崖根治（层名口径唯一性对称判据 + 时间槽位分桶下沉）

`doc-1790160473728-0afe2740` 综合 87 / 目标 95，**事实完整与一致性仍是 40 分**。
`factIntegrity` 统计 `fact_consistency` 类别的**全部** error，其中 **8 条是「工序规格冲突」**：

- **层名口径唯一性判据的对称补全**：原判据只要求**资料侧**口径唯一
  （`sourceThicknesses.length === 1`），却拿资料里唯一的「垫层 120mm」要求正文每一处「垫层」
  都等于 120mm——而 `垫层` 在道路/管沟/地坪/基础下本就是不同对象、不同厚度，
  实测正文出现 50/100/200/10/30/300/150/5mm 八种取值 → 8 条 error。
  现与资料侧同源规则镜像：**正文侧同一层名出现 >1 个不同厚度/配比 ⇒ 层名不再唯一指代某一对象
  ⇒ 跳过确定性比对**，且**显性记录不静默**（逐层 warning 说明跳过原因与取值清单，供人工复核）。
  召回损失已权衡：正文写对一处写错一处（2 种取值）时也不再报，但该场景在层名不含对象限定时
  本就不可裁决——与其拿同一份资料值批量改写正确内容（历史事故：正文防水层 250/30/100mm 被批量改成 3mm），
  不如如实标注「无法裁决」交人工。

**残留 3 条多值冲突逐条根治（均为判据分裂：真值层有、对账侧没有）**：

- **时间槽位分桶下沉**：`330日历天` vs `计划开工日期：2026年10月10日（…）` 被判「计划工期 存在多个值」。
  真值层早已按形态分桶（D-T4 ④），对账侧没有。现 `temporalValueKind` / `stripFactLabelPrefix` /
  `TEMPORAL_DATE_VALUE_RE` 迁入 `factValueNoise` 单源，两侧共用；并修「日期里的 `10月` 被时长正则
  当成时长」的伪装（日期形态先行识别）。
- **尾随形式括注剥离**：`巢湖执珩建设投资有限公司（盖单位章）` 与裸名是同一实体，只剥尾部、
  单组 ≤12 字（中间括注如「（中国科大英才智慧创业基地）」是名称组成部分，不剥）。
- **缺省声明词表扩展**：`未识别`/`未获取`/`未解析`/`未提取` 未在 `ABSENCE_DECLARATION_RE` 中，
  「未识别」曾被当作与真值并列的"另一个值"。

测试：新增 qualityValidation 层名口径对称判据 3 项（含「单值正文仍照旧裁决」的召回守卫与
「多值跳过不改写正文」）、factConsistencyService 残留族回放 5 项（含真冲突仍报的防过度断言）、
单源守卫扩展到 5 个判据；typecheck/lint 干净；全量 14,985 passed | 5 skipped。
