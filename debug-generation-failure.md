# Debug Session: generation-failure

Status: [OPEN]
Session ID: generation-failure
Started: 2026-08-16

## Symptom
用户反馈：服务已启动，最新生成记录仍显示章节生成失败；生成耗时超过一小时且最终未成功生成文件。

## Constraints
- 在收集运行时证据前，不修改业务逻辑代码。
- 第一阶段只读取生成记录、系统日志、节点耗时和错误栈。
- 若需要修改，第一处代码修改只能是调试埋点。

## Falsifiable Hypotheses
1. H1：章节节点实际被标记为 failed/fallback，导致最终流程判定失败，而不是 LLM 调用真正失败。
2. H2：章节生成某个子流程长时间挂起或串行等待，例如项目图谱、小节规划、小节分组、补写或质量修复，导致总耗时超过一小时。
3. H3：最新发布后的依赖/打包改动影响运行时模块解析或导出/保存流程，导致正文已生成但最终资产生成失败。
4. H4：质量门禁或小节完整性修复逻辑过严，把可用正文判定为失败，阻断最终保存/导出。
5. H5：取消/旧任务/并发任务状态污染了最新生成记录，页面看到的是旧失败节点或重复节点。

## Evidence Log
- 最新记录：doc-1786868274828-d13f7cc3，status=warning，elapsedMs=5,829,741（约97分钟），error=生成任务已中断。
- 章节完成：只完成 2/3 章；第三章“确保人、材、机的保障体系与措施”阶段 status=failed，progress.label=小节分组。
- 第三章失败 message=生成任务已中断，请点击继续生成或重新生成；没有 checkpoint 章节内容。
- 第一、二章节点仍显示“参考目标 7600/首轮质量目标 8000”，说明运行中的服务未体现最后的动态目标文案，或加载的是旧构建/旧包。
- 系统日志接口返回空数组，现有日志不足以判断第三章组内哪一步超时。
- 新真实自测 doc-1786874842133-8a64642a：运行 221 秒仍停在 project-graph，partialChapters=[]，说明前置图谱阶段本身已严重拖慢首章生成。H2 被部分确认：不仅第三章，小节生成前的项目图谱也存在长阻塞。

## Fix Plan
1. 已确认 H2：项目图谱前置阶段在真实模板中 221 秒仍未结束，导致正文完全未开始。
2. 最小修复：为项目图谱阶段增加 60 秒硬上限，超时后标记 fallback 并继续普通证据检索/章节生成。
3. 验证结果：doc-1786875190073-433ab6d4 在 60 秒图谱降级，88 秒进入章节生成，279 秒完成第一章并进入第二章。
4. 新证据：第一章部分小节组耗时 75 秒但返回 0 字；第二章 44 小节被拆成 2 个 22 小节大组，每组目标 3600，稳定性和质量都差。
5. 第二个最小修复：超多小节不再压成 2 个大组，恢复为 6～8 小节稳定分组；组返回空时生成可见的需重生成小节块，避免静默缺章。
6. 新证据：doc-1786875655444-74fd52a6 第一章小节组 75～106 秒后 LLM 返回 0 字，只产出 133/676 字兜底，说明组级 LLM 超时后仍慢且兜底质量不可接受。
7. 第三个最小修复：组级目标降到 2600/3200 字，组超时降到 45～75 秒；兜底改为基于资料事实的正式正文，不再输出“需重生成”占位。
8. 新证据：doc-1786877477124-9c673862 与隔离自测均显示章节已完成后进入默认收尾，`rebuildDefaultFinalState` 约 0.5 秒完成，但默认路径继续把约 68 个非阻断 warning 交给多轮 LLM 质量修复，导致文件迟迟不完成。
9. 第四个最小修复：默认快速收尾路径只对 `error + export blocking` 问题触发 LLM 修复；warning 仅进入校验报告，不阻断导出和保存。
10. 新证据：`extractFactsWithLlm` 单次事实抽取耗时 208 秒，且多模态文件理解因未知 provider `deepseek-v4-pro` 打错误栈；两者均为增强步骤，不应阻断成文。
11. 第五个最小修复：LLM 事实抽取增加 45 秒硬超时和 token 限制；不支持多模态的 provider 直接标记 skipped，不再输出错误栈。

## Verification
待修复后用真实模板完整运行验证。
