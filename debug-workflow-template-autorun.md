# Debug Session: workflow-template-autorun

Status: [OPEN]

## Objective
启动 Web 服务，在工作流模板页面对应的“徽光阁项目”模板上运行生成，实时监控生成记录和日志。如发现失败或质量问题，基于日志定位根因、修复、再次运行，直到成功生成正确且高质量专业文档。随后进行代码审查、质量检查、性能/冗余/隐藏问题排查和修复，最终自测通过后发布 npm 正式版本。

## Hypotheses
1. 当前代码修复后，徽光阁项目模板可以成功通过导出门禁，但仍可能出现章节质量、事实一致性或专业性不足问题。
2. 若生成失败，失败更可能发生在质量门禁/事实一致性/章节规划阶段，而不是 Web 服务启动或接口崩溃。
3. 之前的强制小节误判、施工方案误伤、工期日期误判可能已修复，但需要通过新生成记录证明。
4. 若生成速度慢或质量不稳，可能存在补写循环、质量校验过严、重复修复或不必要的 LLM 调用。
5. 发布前仍可能存在隐藏的类型、lint、构建、打包或版本冲突问题，需要完整检查。

## Evidence
待收集。

## Iterations
待记录。

## Final Verification
待完成。
