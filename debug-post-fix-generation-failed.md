# Debug Session: post-fix-generation-failed

Status: [OPEN]

## Problem
用户在 Web 服务已启动后重新跑了一条生成记录，该记录失败。需要全面分析生成记录日志、系统日志和相关产物，找出根本原因，并给出理解与方案。本轮已在用户确认后进行代码修复。

## Hypotheses
1. 最新失败仍是生成完成后的导出门禁/质量校验阻断，而非接口崩溃。
2. 上一轮新增的修复逻辑没有作用到本次生成路径，例如走了继续生成旧链路、旧服务进程未重启或执行路径未触发。
3. 修复逻辑已生效，但仍有未覆盖的硬阻断，例如章节空洞、事实反查、正式表格、禁用词或跨章一致性。
4. 修复逻辑引入了新的质量问题，例如自动补表内容缺少资料事实或替换数字导致事实不足。
5. 本次失败是 LLM、文件解析、知识库、外部依赖或系统异常导致，错误记录在系统错误日志或执行阶段中。

## Evidence
- 最新失败记录：`/Users/pan/.customize-agent/projects/3c3f04667c69/generatedDocuments/drafts/doc-1786129481831-1d672f8c.json`。
- 记录状态：`failed`；标题：`徽光阁施工组织设计`；模板：`徽光阁项目`。
- 系统错误日志 `~/.customize-agent/logs/errors.jsonl` 没有本次生成接口崩溃证据；旧日志主要是 healthCheck、Next cache pack ENOENT。
- 本次结果相比上次已有改善：error 从 18 降到 9；正式表格不足、禁用词、无法反查数字等上一轮问题已不再是阻断。
- 导出门禁仍失败，`exportGate.blockingIssues` 只有 4 个：
  1. 目录与正文不一致，目录小节未在正文中找到：主要承包人案。
  2. 缺少规划小节：主要施工方案。
  3. 跨章一致性冲突：正文出现与资料工期不一致的表述 8月。
  4. 正文缺少规划小节：主要施工方案。
- 正文证据：`主要施工方案` 出现 0 次；`主要承包人案` 出现 2 次；`承包人案` 出现 14 次。
- 代码证据：`documentGenerator.ts` 中 `.replace(/施工方/gu, '承包人')` 会把 `施工方案` 误替换成 `承包人案`。
- 工期证据：正文中存在 `计划开工日期：2026年8月8日`，这是开工日期事实，不是工期时长；但跨章一致性规则把 `8月` 当作与 `45日历天` 冲突。
- 提示词证据：用户提示词中 `应急预案` 是第二章强制挂靠；`雨季/冬季/高温/台风/大风等特殊气候` 是条件性判断要求，不应进入 `requiredSections`。

## Root Cause
1. 主根因：禁用词确定性替换过宽。
   - `.replace(/施工方/gu, '承包人')` 会匹配普通词 `施工方案` 的前三个字。
   - 导致 `主要施工方案` 被改成 `主要承包人案`。
   - 模板规划仍要求 `主要施工方案`，所以结构校验判断：缺少规划小节、正文缺少规划小节、目录与正文不一致。

2. 次根因：工期一致性规则过宽。
   - 正文中的 `2026年8月8日` 是计划开工日期。
   - 规则只看到 `8月`，将其当作与 `45日历天` 的工期口径冲突。

3. 补充根因：提示词结构解析过宽。
   - `应急预案` 确实是强制小节。
   - `雨季/冬季/高温/台风/大风等特殊气候` 来自“判断是否涉及”的条件性要求，被误解析为强制小节。

## Applied Changes
1. 修复后处理替换误伤
   - 文件：`apps/server/src/services/document-workflow/documentGenerator.ts`
   - 收窄 `施工方` 替换边界，只替换主体语境。
   - 增加 `承包人案 -> 施工方案` 的误伤恢复。

2. 修复工期日期误判
   - 文件：`apps/server/src/services/document-workflow/qualityValidation.ts`
   - 新增工期时长提取函数，只比较 `45日历天`、`工期45天`、`总工期2个月` 等明确时长。
   - 不再把 `2026年8月8日` 中的 `8月` 当作工期值。

3. 修复提示词强制小节误判
   - 文件：`apps/server/src/services/document-workflow/chapterGeneration.ts`
   - 新增条件性上下文识别：`判断是否涉及`、`若涉及`、`根据项目所在地气候特征`、`根据计划施工周期`、`专项小节` 等。
   - 条件性气候要求不再进入 `requiredSections`。
   - 解析标题时排除 `雨季/冬季/高温/台风/大风等特殊气候` 这类条件性专项词。

4. 强化事实一致性降噪
   - 文件：`apps/server/src/services/document-validation/factConsistencyService.ts`
   - 补充排除投标文件制作、电子服务系统、交易平台、监督部门等流程噪声。
   - 工期事实比较只保留明确工期时长参与归一化。

5. 优化门禁日志展示
   - 文件：`apps/server/src/pages/documents/index.tsx`
   - 工作流抽屉的门禁失败提示优先展示 `blockingIssues.message`，不只展示 checklist 概括项。

## Verification
- VS Code diagnostics：相关文件均无错误。
- `pnpm --filter @customize-agent/server lint`：通过。
- 针对当前失败正文抽样验证：应用新替换逻辑后，`主要施工方案=2`，`主要承包人案=0`，`承包人案=0`。
- 工期抽样验证：当前正文只提取到 `45日历天`，不再把 `8月` 作为工期值。

## Next Step
请重启/热更新 Web 服务后，在页面点击当前失败记录的“继续”，或重新生成一条记录。预期：
- 不再出现 `主要承包人案`。
- 不再提示缺少 `主要施工方案`。
- 不再提示 `8月` 与 `45日历天` 工期冲突。
- “内容优化与质量校验”不再把 `雨季/冬季/高温/台风/大风等特殊气候` 显示为强制小节，只应保留真正强制的 `应急预案`。
