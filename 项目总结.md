# 专业长文档生成智能体 · 项目总结

## 一、领域与定位

**领域**:建筑工程投标领域,核心场景是"施工组织设计"——投标技术标的核心文档,单份 2~20 万字、20+ 章节,由评标专家按评标办法逐项打分。

**定位**:一个**本地化**的 AI 文档生成与知识库工作台。用户把项目资料(招标文件、工程量清单、CAD 图纸、合同、补疑答疑)上传到本地知识库,配置好提示词、角色和文档规范包后,一键生成**可校验、可追溯、可导出**的正式文档。

## 二、做了什么

1. **本地知识库引擎**:PDF/Word/Excel/图片/OCR 内容抽取、切片、去重、向量化(本地或远端 Embedding)、HNSW 向量库 + 关键词/向量混合检索 + 重排序,支持任意 OpenAI 兼容 / Anthropic / Google / Ollama 模型端点;
2. **文档生成工作流引擎**(核心,约 2.2 万行 TypeScript、70 个模块):从资料理解、事实抽取、章节规划、并行写作、审查修复到最终门禁的完整生成管线;
3. **质量校验与门禁体系**:生成中和导出前自动执行 40+ 项校验,通过导出门禁后放行导出;
4. **施组领域知识库**:评标必查结构、标准模块、施工工艺知识卡、标准表格库、六维评分的结构化沉淀,在生成各阶段自动注入并校验;
5. **管理控制台与 CLI**:知识库/提示词/角色/规范包/模板管理、生成过程监控、门禁与质量报告展示,以及一个通用终端 AI 助手;
6. **导出**:Markdown / HTML / DOCX / PDF。

## 三、系统架构

整个项目是一个 monorepo,由三部分组成:

**应用层(apps)** 有两个应用:一是 Next.js 管理控制台,负责知识库、提示词、角色配置、文档规范包、工作流模板的管理,以及生成过程监控、门禁与质量报告的展示和文档导出;二是一个终端 AI 助手,支持原生 Function Calling、双语 TUI 界面,按 reasoning / action / reader 三级模型分层路由。

**基础包层(packages)** 是共享基础设施:本地知识库引擎负责资料的内容抽取、切片、去重、向量化与混合检索;模型层是统一的多协议 Provider 工厂,对接 OpenAI 兼容 API、Anthropic、Google、Ollama 等任意端点,并提供统一的重试、限流与调用统计能力。

**文档生成工作流引擎(核心)** 位于服务端,约 2.2 万行 TypeScript,是项目的核心成果。它把整个生成过程编排为七个阶段:准备 → 资料治理 → 结构规划 → 并行生成 → 审查修复 → 全局一致性检查 → 最终门禁与导出。技术栈为 TypeScript + Next.js + better-sqlite3(WAL 模式)+ hnswlib-node 本地向量库,数据全部本地化,不依赖任何云端服务。